# React Redux basic counter

Exemple simple de React + Redux utilisant un compteur.

La sauvegarde du compteur se fait dans le store.
Le middleware `logger` permet de logger tous les changements du store.